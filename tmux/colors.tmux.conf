if-shell '[ -n "$SSH_CONNECTION" ]' \
  'source-file $HOME/dotfiles/colors/default/tmux.conf' \
  'source-file $HOME/.cache/wal/tmux.conf'

set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[ q'

set-environment -g COLORTERM "truecolor"

set -g status on
set -g status-justify left
set -g status-position bottom
set -g status-left-length 50
set -g status-right-length 100

set -g status-style "fg=#{@wal_fg}"
set -g status-left-style "fg=#{@wal_accent}"
set -g status-right-style "fg=#{@wal_accent}"
set -g message-style "fg=#{@wal_fg}"
set -g pane-border-style "fg=#{@wal_ui_muted}"
set -g pane-active-border-style "fg=#{@wal_accent}"

set -g pane-border-lines heavy

set -g status-left "#[fg=#{?client_prefix,#{@wal_warn},#{@wal_accent}},bold]#[fg=#{@wal_accent},bold] #S · #[default]"
set -g status-right "#[fg=#{@wal_accent}] #{pane_current_path}#[default] | #[fg=#{@wal_accent}] #{user} 󰒋 #{host_short}#[default] | #[fg=#{@wal_accent}]󰃰 %F#[default]"

set -g window-status-style "fg=#{@wal_inactive_text}"
set -g window-status-current-style "fg=#{@wal_accent},bold"

set -g window-status-format "#I → #W"
set -g window-status-current-format " #I → #W"
set -g window-status-separator " | "
